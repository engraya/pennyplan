/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'assets.website-files.com', 'unsplash.com', 'pagedone.io'],
      },
};

export default nextConfig;
